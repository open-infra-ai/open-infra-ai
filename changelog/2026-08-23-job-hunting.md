# 2026-08-23 新增求职执行区（job-hunting/）

- 新增 `job-hunting/` 目录（**active 区，非档案**）：
  - `resume.zh.md` —— 一页中文简历草稿（serving 岗排序，`【】`为待填真实信息），
    bullet 与 E 编号沿用 `interview/` 证据包口径；
  - `companies.md` —— 目标公司清单（大模型公司 / 大厂 AI Infra / 推理基础设施三类）；
  - `tracking.md` —— 投递追踪表 + 每周节奏检查 + 渠道说明。
- README 在「档案区」前新增「求职执行区（active）」小节，指向上述三文件。
- `interview/` 档案保持只读，未改动；简历模板后续更新也不回写档案区（以
  `interview/` 为唯一素材源，`job-hunting/resume.zh.md` 为唯一简历出口）。